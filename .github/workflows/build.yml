name: Build project

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: [windows-2019]

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: msbuild.exe MathLib.sln /t:Restore;Build /p:Configuration=Release
